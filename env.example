# Frontend Environment Variables
# Copy this to .env.local and fill in your values

# Supabase Configuration (Safe to expose)
VITE_SUPABASE_URL=your_supabase_url_here
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Site URL for authentication redirects (Safe to expose)
VITE_SITE_URL=https://ezyum.com

# Analytics (Safe to expose)
VITE_ANALYTICS_ID=your_analytics_id_here

# Backend URL (Safe to expose)
VITE_BACKEND_URL=http://localhost:3001/api

# Note: OpenAI, Hugging Face, and MealDB keys are now handled securely on the backend
# See server/env.example for backend configuration
